export interface Film {
    title: string,
    episode_id: number,
    opening_crawl: string,
    director: string,
    producer: string,
    release_date: string,
    species: number[],
    starships: number[],
    vehicles: number[],
    characters: number[],
    planets: number[],
    url: string,
}
